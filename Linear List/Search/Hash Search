无序到有序的映射
特点：把元素通过哈希函数映射到键值中
提高时间效率


重点：
1.建立哈希函数
2.如何处理碰撞冲突（尤其在插入与删除时）——————需要设定初始值，判断里面有无有效元素

如何处理碰撞冲突？
1.取大一些的M，比取值范围要大
2.想方法解决冲突

散列函数设计方法：
出现冲突时：
（1）开发寻址法
（2）链地址法（高效）一个键值对应一个链表~链表记录所有发生碰撞的元素


哈希表删除后可以添加一个删除标记~不能向上挪
