1.“->”与“.”的区别：->是解引用，适用于指针变量，是到达指针变量地址处的那个变量；.则是直接说明这个变量内部的变量


2.new与delete
为什么我们在新建一个结点时会使用new和delete而非直接生成结点呢？
因为直接设置的是局部动态变量，超过作用域就会被销毁；而通过new设置的是动态分配，不是临时的，所以不delete就存在

ListNode* p=new ListNode{data_,next_}  #new返回的是新分配的地址
delete p:并不代表清空p这个指针变量里的存的地址位置，而是清空指向的地址中的内容！


3.this是指针
