# 最短路径——Dijkstra法思路讲解
## 已知
```text
我们起点走到另一个点的路径一定会比当前存储的所存在的最短路径要长；所以我们只需要每轮确认一个最短路径，再去更新就好
```
## 思路提示
```text
重点在三个数组：
distance数组存当前每个点与起点的距离
vsited数组存当前点与起点的距离是否已经确定
pre数组存每个点的前一个点是谁，便于最后的输出
```
