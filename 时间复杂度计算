int sum=0;
for(int i=1;i<n;i*=2)
{
   for(int j=0;j<i;j++)
   sum++;
}
求算时间复杂度？
A. O(n)  B.O(logn) C.O(nlogn) D.O(n^2)

错解：n*logn   正解：n
想法：找两层嵌套的时间复杂度再相乘
核心特征：每次内层的循环次数与外层相关，所以不太相同。
当内层循环次数不同时，使用累加时，已经潜移默化用了外层循环次数了。

